services:
  - type: web
    name: tro-li-ai-tin-hoc-10
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
